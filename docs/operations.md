# Operations Notes

- Use environment variables for all secrets.
- In production, switch DATABASE_URL to PostgreSQL.
- Enable COOKIE_SECURE=true behind HTTPS.
- After the CI workflow runs once, GitHub Actions badges become visible in both the README and the dashboard.
